name: AI Review

# Thin caller. Logic lives in NERV-es/.github (SHA-pinned).
# Free GitHub Models code review on every PR. Advisory only.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    uses: NERV-es/.github/.github/workflows/ai-review-reusable.yml@6ac4e50fb4c1ca32cf707151d0a69f662f4d0781
    secrets: inherit
