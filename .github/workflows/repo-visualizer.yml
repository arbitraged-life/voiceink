# Org-standard policy workflow — thin caller for the shared reusable in
# NERV-es/.github. Byte-identical across every NERV-es fork.
name: Repo Visualizer

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  diagram:
    uses: NERV-es/.github/.github/workflows/repo-visualizer-reusable.yml@c942996fd330257189a6d85e47d3201509b28e3a
    secrets: inherit