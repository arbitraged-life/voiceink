# Org-standard policy workflow — thin caller for the shared reusable in
# NERV-es/.github. Byte-identical across every NERV-es fork.
name: Close Stale Issues and PRs

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: NERV-es/.github/.github/workflows/stale-reusable.yml@c942996fd330257189a6d85e47d3201509b28e3a
    secrets: inherit