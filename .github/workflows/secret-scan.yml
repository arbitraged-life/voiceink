# Org-standard policy workflow — thin caller for the shared reusable in
# NERV-es/.github. Byte-identical across every NERV-es fork.
name: Secret Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trufflehog:
    uses: NERV-es/.github/.github/workflows/secret-scan-reusable.yml@c942996fd330257189a6d85e47d3201509b28e3a
    secrets: inherit