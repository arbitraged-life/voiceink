interface:
  display_name: "Voice"
  short_description: "Repo-specific patterns and workflows for voice"
  default_prompt: "Use the voice repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true